version: '2'
services:
  sndeals-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/sndeals/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=sndeals
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
